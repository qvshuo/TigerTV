<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Resource sites may still use plain HTTP; allow cleartext globally
         while the upstream MacCMS ecosystem migrates to HTTPS. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
